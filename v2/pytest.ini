[pytest]
addopts = --import-mode=importlib --cov=dar_backup --cov-report=term --cov-report=xml:/tmp/coverage.xml --no-cov-on-fail -m "not live_discord"
testpaths = tests
markers =
    live_discord: runs tests that hit a real Discord webhook (requires DAR_BACKUP_DISCORD_WEBHOOK_URL)
